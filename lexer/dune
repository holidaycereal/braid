(library
  (name braidlexer)
  (foreign_stubs
    (language c)
    (names lexer_stubs lexer helpers)
    (flags (-I .)))
  (modules))

(executable
  (name main)
  (libraries braidlexer)
  (modules lexer main))
